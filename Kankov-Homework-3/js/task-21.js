function strMultiplication (str, number){
    alert(str.repeat(number))
}
const str = prompt("Enter few words");
const number = +prompt("enter number of multiplications")
strMultiplication(str, number)
